<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      .bg_color {
        background: linear-gradient(
          135deg,
          rgba(217, 245, 225, 0.726),
          rgba(138, 175, 240, 0.538),
          rgba(210, 170, 167, 0.7261029411764706)
        );
      }
      .bgcolor {
        background: linear-gradient(
          35deg,
          rgba(247, 207, 204, 0.726),
          rgba(187, 210, 249, 0.538)
        );
      }
      .custom-input {
        border: none;
        border-bottom: 1px solid #000; /* Adjust the color and thickness as needed */
        border-radius: 0;
        outline: none;
        box-shadow: none;
        background-color: transparent;
      }

      .custom-input:focus {
        border-bottom: 1px solid #004793; /* Change color on focus */
        box-shadow: none;
      }

      .form-label {
        position: absolute;
        top: -20px;
        left: 0;
        color: #040404; /* Adjust the label color as needed */
        pointer-events: none;
        transition: all 0.2s ease-out;
      }

      .form-outline {
        position: relative;
        margin-bottom: 1.5rem;
      }

      .form-outline .custom-input:focus + .form-label,
      .form-outline .custom-input:not(:placeholder-shown) + .form-label {
        top: -20px;
        left: 0;
        font-size: 0.75rem;
        color: #007bff; /* Adjust the color on focus */
      }
    </style>
  </head>
  <body style="width: 100%; height: 100%; padding: 0; margin: 0">
    <section class="bg_color">
      <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
          <div class="col col-xl-10">
            <div class="card" style="border-radius: 1rem; overflow: hidden">
              <div class="row g-0">
                <div class="col-md-6 col-lg-5 d-none d-md-block">
                  <img
                    src="/img/log.png"
                    alt="login form"
                    class="img-fluid"
                    style="border-radius: 1rem 0 0 1rem; height: 100%"
                  />
                </div>
                <div
                  class="col-md-6 col-lg-7 px-4 d-flex align-items-center bgcolor"
                >
                  <div class="card-body p-4 p-lg-5 text-black">
                    <form action="/verifyLogin" method="post">
                      <h5
                        class="fw-normal mb-3 pb-3"
                        style="letter-spacing: 1px"
                      >
                        Sign-in into your account
                      </h5>
                      <% if(msg.length> 0){ %>
                      <div class="alert alert-success"><%= msg %></div>
                      <% } %> <% if(msg1.length> 0){ %>
                      <div class="alert alert-danger"><%= msg1 %></div>
                      <% } %>
                      <div class="form-outline mb-4">
                        <input
                          type="email"
                          name="email"
                          id="form2Example17"
                          class="form-control form-control-lg custom-input"
                          aria-label="Email address"
                          placeholder="Email Address"
                          required
                        />
                      </div>
                      <div class="form-outline mb-4">
                        <input
                          type="password"
                          name="password"
                          id="form2Example27"
                          class="form-control form-control-lg custom-input"
                          aria-label="Password"
                          placeholder="Password"
                          required
                        />
                      </div>
                      <div class="pt-1 mb-4">
                        <button
                          class="btn btn-dark btn-lg btn-block"
                          type="submit"
                        >
                          Login
                        </button>
                      </div>
                      <a class="small text-muted" href="#!">Forgot password?</a>
                      <p class="mb-5 pb-lg-2" style="color: #393f81">
                        Don't have an account?
                        <a href="/registration" style="color: #393f81">
                          Register here
                        </a>
                      </p>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </body>
</html>
